<Window x:Class="VerGen.Tool.UI.Dialogs.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
         FocusManager.FocusedElement="{Binding ElementName=TbResult}"
        Title="输入" Height="140" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Border Padding="10, 5">
        <StackPanel>
            <TextBlock Name="TxtTitle" Margin="0,0,0,10" />
            <TextBox  Name="TbResult" Padding="3,5" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button Name="BtnOk" IsDefault="True"  Content="确定" Padding="20,5" Click="BtnOk_Click" />
                <Button Name="BtnCancel" IsCancel="True" Content="取消" Margin="5,0,0,0" Padding="20,5" Click="BtnCancel_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
